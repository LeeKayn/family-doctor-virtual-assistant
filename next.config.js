/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  
  // Chỉ giữ cấu hình tối thiểu, loại bỏ các phần không cần thiết gây lỗi
  poweredByHeader: false
};

module.exports = nextConfig; 